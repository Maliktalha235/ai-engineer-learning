# Database Module - Weather Tracker

This module handles all database operations for the Weather Tracker application.

## Functions:
1. Create database if doesn't exists already
2. get connection function
3. create table if already not created
4. Insert data into MySQL
